/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "pt.m.wikipedia.org"
        ]
    }
};

export default nextConfig;
